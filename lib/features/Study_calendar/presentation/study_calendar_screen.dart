import 'package:flutter/material.dart';
import 'package:study_flow/features/Study_calendar/presentation/widgets/study_calendar_body.dart';

class StudyCalendarScreen extends StatelessWidget {
  const StudyCalendarScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: StudyCalendarBody());
  }
}
