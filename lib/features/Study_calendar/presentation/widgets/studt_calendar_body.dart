import 'package:flutter/material.dart';

class StudtCalendarBody extends StatelessWidget {
  const StudtCalendarBody({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
